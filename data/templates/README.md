# Data templates

Small labeled examples committed to git for reference and testing.

| File | Description |
|------|-------------|
| `curated_edge_cases.jsonl` | Short/casual boundary examples (87 rows) |

These were merged into the local training set (`data/raw/all.jsonl`). The full dataset is not committed.
